@{
    ViewData["Title"] = "Access Denied";
}

<div class="empty-state text-center">
    <i class="bi bi-shield-lock display-3 text-warning"></i>
    <h1 class="mt-3">Access denied</h1>
    <p class="text-muted">You do not have permission to access that page.</p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-accent">Return Home</a>
</div>
